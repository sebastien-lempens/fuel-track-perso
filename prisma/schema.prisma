generator client {
  provider = "prisma-client-js"
}

datasource db {
  provider = "sqlite"
  url      = env("DATABASE_URL")
}

model RawFuelEntry {
  id              String   @id @default(cuid())
  date            DateTime 
  liters          Float
  priceTotalLiter Float
  odometer        Int
}
